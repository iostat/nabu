project(":client-test") {
    group 'io.stat.nabuproject'
    version '0.1-SNAPSHOT'

    apply(plugin: 'application')
    mainClassName = 'io.stat.nabuproject.client_test.Main'

    dependencies {
        compile(project(':nabu'))
    }
}

